# Nebula Raiders – Flask Game (No Database by default)

A procedurally generated top-down shooter game made using HTML Canvas + Vanilla JS.

## Run Locally

1. Create & activate virtualenv (optional)
2. Install dependencies:
