# Nebula Raiders – Flask Game (No Database)

A procedurally generated top-down shooter game made using HTML Canvas + Vanilla JS.

## Run Locally
pip install -r requirements.txt
python app.py


Open http://localhost:5000

## Deploy on Railway
- Upload this repo or connect GitHub
- Railway auto-detects Flask
- Procfile already included
- App will run successfully at the generated URL

## Save System
- Game saves progress in browser localStorage (no server database needed).